#include<stdio.h>

void prime(int num)
{
    for (int i = 0; i < num; i++)
    {
        if (num % i)
    }
}
int main()
{
    int num;
    int maxLength = 50;
    int array[maxLength];
    printf("Nhap n:");
    scanf("%d", &num);
    prime(num);
    

}